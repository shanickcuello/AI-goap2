using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RobotFSMManager : MonoBehaviour
{
    /*
    FSM<string> _fsm;

    StateIdle<string> idle = new StateIdle<string>(gameobject);
    StatePatrol<string> patrol = new StatePatrol<string>();

    
    // Start is called before the first frame update
    void Start()
    {
        idle.AddTransition("IdlePatrol", patrol);
        patrol.AddTransition("PatrolIdle", idle);

        _fsm = new FSM<string>(patrol);
    }

    // Update is called once per frame
    void Update()
    {
        _fsm.OnUpdate();
      

      

    }*/
}
